# ===============================================
# AI-SERVICE - Configuración de Variables de Entorno
# ===============================================

# Puerto del servicio
PORT=8081

# API Keys para autenticar requests desde el backend (separadas por comas)
# Debe coincidir con AI_SERVICE_API_KEY en el backend
API_KEYS=dev-key-12345

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_ORG_ID=
# Timeout en ms para requests a OpenAI
OPENAI_TIMEOUT_MS=15000

# Configuración de IA
AI_MIN_CONFIDENCE=0.6

# Rate Limiting (requests por minuto por IP)
RATE_LIMIT_PER_MIN=120

# Logging
LOG_LEVEL=info
# Opciones: trace, debug, info, warn, error, fatal

# CORS Origins (separados por comas)
CORS_ORIGIN=http://localhost:3000,http://localhost:4200

# Environment
NODE_ENV=development
# Opciones: development, production, test, staging

# Mock Mode (para testing sin OpenAI)
USE_OPENAI_MOCK=false

# ===============================================
# Notas:
# ===============================================
# 1. Copia este archivo como .env en la raíz del ai-service
# 2. Obtén tu OPENAI_API_KEY en: https://platform.openai.com/api-keys
# 3. El modelo recomendado es gpt-4o-mini (más económico y rápido)
# 4. Para testing sin consumir API de OpenAI: USE_OPENAI_MOCK=true
