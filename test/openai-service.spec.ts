import { describe, it, expect, vi, beforeEach } from 'vitest';
import * as openaiService from '../src/ai/openai-service';

beforeEach(() => {
  vi.restoreAllMocks();
});

describe('openai-service', () => {
  it('should ', async () => {});
});
